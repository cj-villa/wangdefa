## 1. 分页配置统一

- [x] 1.1 盘点并定位所有分页入口（列表页、表格页、查询结果页）
- [x] 1.2 抽取并统一分页大小选项常量为 `10/20/50`
- [x] 1.3 替换各分页页面硬编码分页大小选项为统一配置

## 2. 分页行为对齐

- [x] 2.1 确保切换分页大小后请求参数 `pageSize` 正确透传
- [x] 2.2 对齐分页状态更新逻辑，保证列表刷新行为一致
- [x] 2.3 确认接口协议不变（`current`、`pageSize` 兼容现有实现）

## 3. 测试与质量校验

- [x] 3.1 增加关键分页页面的分页大小切换测试
- [x] 3.2 增加分页参数回归校验（10/20/50）
- [x] 3.3 执行并通过 `eslint` 与 `tsc` 校验
