## 1. 现状确认与契约对齐

- [x] 1.1 定位交易创建前端提交 payload 与后端 DTO 校验规则，明确 `amount` 与日期字段的当前不一致点。
- [x] 1.2 确认交易创建接口最终字段契约（amount 作为 number string；日期字段为后端可稳定转换的日期值）。

## 2. 前端提交格式修复

- [x] 2.1 调整新增交易表单提交逻辑：将 `amount` 序列化为数字字符串后再调用创建接口。
- [x] 2.2 调整 `transactionDate` 与 `ensureDate` 提交格式，确保请求值可被后端 DTO 转换为 Date。
- [x] 2.3 保持编辑交易与新增交易在字段序列化规则上一致，避免链路行为分叉。

## 3. 后端 DTO 与校验链路修复

- [x] 3.1 在交易创建 DTO 中补充/校正日期字段的转换与校验组合，确保合法值通过、非法值拒绝。
- [x] 3.2 校正金额字段校验与类型声明，确保 number string 输入可通过校验并进入业务逻辑。
- [x] 3.3 验证 controller/service 不依赖旧输入类型，避免修复后出现隐式类型错误。

## 4. 回归验证与交付

- [x] 4.1 验证新增交易成功路径：前端提交后接口成功创建，不再出现三类校验错误。
- [x] 4.2 验证失败路径：无效 amount/日期输入时返回明确校验错误且不入库。
- [x] 4.3 执行 lint、tsc、build 校验并记录变更说明。
