## Regression Checklist

- [x] 理财列表筛选区不再包含“昨日收益/余额”筛选项
- [x] 理财列表仍保留“昨日收益/余额”展示列，不影响浏览与排序
- [x] 理财列表名称/编码/渠道筛选可用，且一键清空有效
- [x] 理财交易列表支持名称筛选并返回匹配数据
- [x] 理财交易列表支持编码筛选并返回匹配数据
- [x] 理财交易列表支持交易类型筛选并返回匹配数据
- [x] 理财交易列表支持日期范围筛选并返回匹配数据
- [x] 理财交易列表筛选标签显示正确，删除单个标签可触发重新查询
- [x] 列表请求失败时展示错误提示和重试入口，筛选输入不丢失

## Build Validation

- [x] `yarn build:fe` 通过（仅保留既有 bundle size warnings）
- [x] `yarn build:be` 通过

## Notes

- 后端交易列表 query 扩展了 `name`、`code` 筛选参数，并保持旧参数兼容。
- 前端 financial 两个 tab 采用一致的“应用筛选 / 一键清空 / 活跃筛选标签 / 错误重试”交互模式。
