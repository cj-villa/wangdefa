## 1. Frontend fee input support

- [x] 1.1 在 `create-financial.tsx` 新增手续费输入项，默认值设为 `0`，并补充输入校验（非负数）。
- [x] 1.2 更新前端理财类型定义与请求参数，确保 create/update 接口可携带手续费字段。
- [x] 1.3 在理财编辑回填场景验证手续费展示与保存一致性。

## 2. Backend fee field and calculation

- [x] 2.1 扩展后端理财相关 DTO/Entity/接口定义，支持手续费字段并默认 `0`。
- [x] 2.2 调整交易净值/份额计算服务，统一使用 `amount - fee` 作为有效金额参与计算。
- [x] 2.3 对历史缺失手续费数据增加兼容兜底（按 `0` 处理），避免计算异常。

## 3. Verification and quality

- [x] 3.1 增加或更新后端单测，覆盖手续费扣减计算与边界校验场景。
- [x] 3.2 联调验证新增/编辑理财与交易计算结果，确认收益/净值符合预期。（通过表单默认值与回填逻辑检查 + 交易计算单测验证）
- [x] 3.3 执行 eslint、tsc 与后端测试命令，确保提交门禁通过。
